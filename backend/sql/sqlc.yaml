version: "2"
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "queries.sql"
    gen:
      go:
        package: "models"
        out: "../internal/infra/database/models/"
        emit_interface: true
        emit_json_tags: true
        emit_prepared_queries: true

